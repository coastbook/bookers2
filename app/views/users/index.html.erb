<h1>Users</h1>

  <% @users.each do |user| %>
    <p>image</p>
    <%= image_tag user.get_profile_image(100,100) %>
    <p>name</p>
    <%= user.name %>
    <p><%= link_to "Show", user_path(user) %></p>
  <% end %>
